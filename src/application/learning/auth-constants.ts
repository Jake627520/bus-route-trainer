/**
 * Zero-Auth Driver Identifier
 *
 * In the current zero-auth development phase, all API endpoints default to this constant.
 * Client-controlled headers (e.g. x-driver-id) are strictly forbidden from acting
 * as authentication/security selectors.
 */
export const DEFAULT_DRIVER_ID = 'driver_default_local';
