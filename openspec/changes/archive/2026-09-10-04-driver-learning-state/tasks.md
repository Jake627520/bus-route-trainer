# Tasks: 04-driver-learning-state

## TDD Implementation Sequence (RED → GREEN → REFACTOR)

- [x] Task 01: Define domain entities (`DriverVariantProgress`, `LearningCard`), enums (`ProgressStatus`, `CardType`, `CardState`), and deterministic `cardKey` generator in `src/domain/learning/` <!-- id: 04-dls-01 -->
- [x] Task 02: Write domain unit tests verifying: (1) deterministic key generation for `STOP` and `NEXT_STOP`, (2) Test A (topology semantics): `STOP` card identity remains stable when stops are inserted elsewhere or renumbered, whereas `NEXT_STOP` identity is strictly based on adjacent pair `(fromStopId, toStopId)` such that inserting intermediate stop X into A -> B breaks adjacency and produces new transition cards A -> X and X -> B while unaffected edge B -> C remains identical, (3) single-stop boundary case (`length === 1` produces 1 STOP and 0 NEXT_STOP cards), and (4) aggregate status: default status is `NOT_STARTED` and aggregate validates valid enum values without premature status machine mutation (`card-key-generator.test.ts`, `driver-variant-progress.test.ts`) <!-- id: 04-dls-02 -->
- [x] Task 03: Define `LearningProgressRepository` port interface with atomic transaction contract and associated DTOs in `src/application/learning/learning-progress-repository.port.ts` <!-- id: 04-dls-03 -->
- [x] Task 04: Define `DriverVariantProgress` and `LearningCard` models in `prisma/schema.prisma` with zero foreign keys to GTFS tables, composite uniqueness `[driverId, targetVariantKey]` and `[progressId, cardKey]`, and generate Prisma client <!-- id: 04-dls-04 -->
- [x] Task 05: Write integration tests for `PrismaLearningProgressRepository` verifying: (1) atomic transaction contract: forced failure during card insertion triggers 100% rollback leaving 0 progress and 0 cards in DB, (2) composite uniqueness enforcement, (3) cascade deletion from progress to cards, (4) cross-driver data isolation (Alice vs Bob), and (5) GTFS table independence (purging GTFS tables leaves learning records completely intact) (`prisma-learning-progress-repository.test.ts`) <!-- id: 04-dls-05 -->
- [x] Task 06: Implement `PrismaLearningProgressRepository` in `src/infrastructure/learning/prisma-learning-progress-repository.ts` turning repository tests green <!-- id: 04-dls-06 -->
- [x] Task 07: Write unit tests for `EnrollVariantUseCase` verifying: (1) generation of `STOP` cards for each station and `NEXT_STOP` cards for each adjacent pair, (2) sequential idempotency returning existing progress without duplicating cards, (3) Test B (concurrent enrollment): `Promise.all([enroll(driverA, variantX), enroll(driverA, variantX)])` resolves to exactly 1 `DriverVariantProgress` and exactly N cards, returning status 200 existing progress without surfacing P2002 or 500 errors, and (4) 404 `VARIANT_NOT_FOUND` when the target variant does not exist in GTFS (`enroll-variant-use-case.test.ts`) <!-- id: 04-dls-07 -->
- [x] Task 08: Implement `EnrollVariantUseCase` in `src/application/learning/enroll-variant-use-case.ts` with `DEFAULT_DRIVER_ID` and concurrent conflict resolution <!-- id: 04-dls-08 -->
- [x] Task 09: Write unit tests for `GetVariantProgressUseCase` verifying: (1) retrieval of progress and learning cards, (2) dynamic projection of `currentSequence` onto cards derived from current variant topology (`stopId` sequence for STOP; `fromStopId` departure sequence for NEXT_STOP), (3) 404 `PROGRESS_NOT_FOUND` when the driver has not enrolled, and (4) topology deletion/unavailability resilience: if GTFS variant is unresolvable, returns cards with `currentSequence: null` and never deletes progress records (`get-variant-progress-use-case.test.ts`) <!-- id: 04-dls-09 -->
- [x] Task 10: Implement `GetVariantProgressUseCase` in `src/application/learning/get-variant-progress-use-case.ts` <!-- id: 04-dls-10 -->
- [x] Task 11: Write integration tests and implement Next.js Route Handler `POST /api/progress/enroll` verifying 201 for new enrollment, 200 for idempotent repeat, and 404 `{ error: { code: 'VARIANT_NOT_FOUND', ... } }` for nonexistent GTFS variants (`progress-enroll-api.test.ts`, `src/app/api/progress/enroll/route.ts`) <!-- id: 04-dls-11 -->
- [x] Task 12: Write integration tests and implement Next.js Route Handler `GET /api/progress/[variantKey]` verifying: (1) 200 payload with cards for enrolled variant, (2) 404 `{ error: { code: 'PROGRESS_NOT_FOUND', ... } }` when unenrolled, and (3) Test C (URL encoding round-trip): handles `variantKey` containing `>` encoded as `%3E` using framework-provided decoded parameter without double-decoding (`progress-query-api.test.ts`, `src/app/api/progress/[variantKey]/route.ts`) <!-- id: 04-dls-12 -->
- [x] Task 13: Write end-to-end integration test verifying vertical slice: import GTFS -> query variants -> enroll driver with URL-encoded variantKey -> retrieve progress with dynamically projected cards (`fromStop` sequence for NEXT_STOP) -> verify concurrent enrollment idempotency -> verify cross-driver isolation (`learning-state-e2e.test.ts`) <!-- id: 04-dls-13 -->
- [x] Task 14: Run full verification suite (`npm test`, `npm run lint`, `npm run build`, `npx prisma validate`, `openspec doctor`, `git diff --check`) confirming 0 regressions, clean boundary, and 0 lint errors <!-- id: 04-dls-14 -->
